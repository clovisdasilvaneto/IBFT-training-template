import measures from "./config/measures";
import palette from "./config/palette";
import { IBFTTheme } from "./config/types";
import typography from "./config/typography";

const theme: IBFTTheme = {
  measures,
  palette,
  typography,
};

export default theme;
